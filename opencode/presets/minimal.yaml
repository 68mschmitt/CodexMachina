name: minimal
version: 1
description: Minimal preset with essential gates only

gates:
  - run.tests: pass
  - run.lint: pass

agents:
  - Facilitator
  - Planner
  - Implementer

tools:
  - repo.search
  - repo.map
  - run.tests
  - run.lint
